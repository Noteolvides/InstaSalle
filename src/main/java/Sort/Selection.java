package Sort;
/*
public class Selection {
    public static void SelectionSort(int a[], int n){
        int minim;
        int aux;

        for(int i = 0; i < n-1; i++) {
            minim = i;
            for (int j = i + 1; j < n; j++) {
                if (a[j] < a[minim]){
                    minim = j;
                }
            }
            aux = a[minim];
            a[minim] = a[i];
            a[i] = aux;
        }
    }
}
*/
